export interface UserFilterParams {
    maxPageSize: number;
    pageNumber: number;
    pageSize: number;
    userId: number;
    currentUsername: string;
    gender: string;
    minAge: number;
    maxAge: number;
}
